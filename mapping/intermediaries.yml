intermediaries:
  queries:
    - csv_url: 'data/nodes-intermediaries.csv'
      entities:
        intermediary:
          schema: LegalEntity
          keys:
            - _id
          properties:
            name:
              column: name
            address:
              column: address
            country:
              column: countries

address-relations:
  queries:
    - csv_url: 'data/relationships.csv'
      filters:
        _type: "registered_address"
      entities:
        address:
          schema: UnknownLink
          keys:
            - _start
            - _end
            - link
          properties:
            startDate:
              column: start_date
              format: "%d-%b-%Y"
            endDate:
              column: end_date
              format: "%d-%b-%Y"
            subject:
              column: _start
            object:
              column: _end

relations:
  queries:
    - csv_url: 'data/relationships.csv'
      filters:
        _type: "officer_of"
      entities:
        officer:
          schema: Representation
          key_literal: 'representation'
          keys:
            - _start
            - _end
          properties:
            startDate:
              column: start_date
              format: "%d-%b-%Y"
            endDate:
              column: end_date
              format: "%d-%b-%Y"
            role:
              column: link
            agent:
              column: _start
            client:
              column: _end
